{
  "buildCommand": "pnpm install --frozen-lockfile && pnpm --filter @investor-app/shared build && pnpm --filter frontend build",
  "outputDirectory": "packages/frontend/dist",
  "framework": null,
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
